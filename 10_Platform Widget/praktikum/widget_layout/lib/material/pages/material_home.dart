import 'package:flutter/cupertino.dart';

class MyMaterialHome extends StatelessWidget {
  const MyMaterialHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is MaterialApp"),
    );
  }
}
